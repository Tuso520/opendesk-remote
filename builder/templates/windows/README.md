# Windows Runner Template

Future runner for Windows custom client builds. It must inject BuildSpec,
branding assets, server config, and signing configuration without removing
upstream license notices.

