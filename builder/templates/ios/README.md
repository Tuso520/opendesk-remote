# iOS Runner Template

Future runner for iOS arm64 builds. Requires macOS runner, Apple Developer
account, bundle id, certificates, and provisioning profiles. First version must
not promise complete controlled-device capability until verified.

